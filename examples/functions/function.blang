0
word increment 1 + endword
word twice increment increment endword
0 ? counter 
0 ? value
while
	counter increment 
	? counter drop
	counter 20 = if
		breakloop
	end
	value twice print 10 charprint ? value drop 
endloop

endscript .
