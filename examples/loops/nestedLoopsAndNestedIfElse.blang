0 ? a
0 ? b 
while
a 1 + ? a
	10 a < dup if
		100 a - print
	end
	else
		breakloop
	end
while
b 1 + ? b
	10 b < dup if
		b print
	end
	else
		0 ? b
		1234 print
		breakloop
	end
endloop
endloop
endscript .
